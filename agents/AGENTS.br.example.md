# AGENTS · brain-researcher example (worked example, stub)

This file is a worked end-to-end example showing the full closed loop:

> Discover BR tools → preflight → validate → execute recipe → self-critique → grounding gate → final report.

**Status: placeholder for v0.1.0.** Populated by the first W6 demo (`examples/plan-validate-and-execute/` in this kit). When that demo lands, this file replays its narrative as a single agent transcript with each step annotated.

For now, see:

- `AGENTS.brain-researcher.md` for the full tool inventory.
- The first available demo under `examples/` once W6 ships.
