# User-facing prompt that triggers the self-critique path

> "Here's our initial finding (see `initial_findings.json`). The whole-cohort correlation is null. Before we write this up as a final null, run a critique pass: is the null a real null, or is it hiding signal in a subgroup or condition? Propose at most one exploratory follow-up and clearly label what's confirmatory vs exploratory in the final report."
